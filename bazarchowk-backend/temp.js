const API_BASE = 'https://bazarchowk-complete.vercel.app'; // or whatever the production URL is
const fetch = require('node-fetch'); // we can use native fetch in Node 18+

async function seed() {
  const adminEmail = 'admin@example.com'; // Wait, I don't know the admin token.
}
